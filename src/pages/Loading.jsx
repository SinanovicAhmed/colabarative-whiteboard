const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <h2 className="text-xl text-gray-700 animate-pulse">Loading...</h2>
    </div>
  );
};

export default Loading;
